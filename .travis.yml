sudo: false

language: python

python:
  - "3.5"
  - "3.5-dev"
  - "nightly"

env:
  - TEST_TYPE=
  - TEST_TYPE='-t property'
  - TEST_TYPE='-t unit'

before_install:
  - cp instance/configuration_example.py instance/configuration.py

install:
  - pip install -r requirements.txt
  - pip install codecov

script:
  - python orchard.py test $TEST_TYPE
  - python orchard.py lint

after_success:
  - codecov
